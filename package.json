{
  "name": "fiscalcr",
  "version": "0.1.0",
  "description": "AI-powered, model-agnostic code review for GitHub",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "generate:prompts": "node scripts/embed-prompts.mjs",
    "dev": "pnpm generate:prompts && tsx watch src/index.ts",
    "build": "pnpm generate:prompts && tsc",
    "build:action": "pnpm generate:prompts && ncc build action/index.ts -o action/dist",
    "start": "node dist/index.js",
    "test": "pnpm generate:prompts && vitest run",
    "test:watch": "pnpm generate:prompts && vitest",
    "lint": "pnpm generate:prompts && tsc --noEmit",
    "clean": "rm -rf dist action/dist"
  },
  "keywords": [
    "code-review",
    "github-action",
    "github-app",
    "ai"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@hono/node-server": "^1.19.9",
    "@octokit/app": "^15.1.0",
    "@octokit/rest": "^21.0.0",
    "@octokit/webhooks": "^13.4.0",
    "hono": "^4.6.0",
    "minimatch": "^10.0.0",
    "pino": "^9.5.0",
    "yaml": "^2.8.2",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@actions/core": "^1.11.0",
    "@actions/github": "^6.0.0",
    "@types/node": "^22.10.0",
    "@vercel/ncc": "^0.38.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
